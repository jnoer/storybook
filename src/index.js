import {
         css,
         config,
         createTheme,
         getCssText,
         globalCss,
         keyframes,
         styled,
         theme,
       } from './stitches.config.js'

console.log('test');
let x = 'test';
console.log(x);
export { Button } from './stories/Button';
export {css,
                 config,
                 createTheme,
                 getCssText,
                 globalCss,
                 keyframes,
                 styled,
                 theme}
